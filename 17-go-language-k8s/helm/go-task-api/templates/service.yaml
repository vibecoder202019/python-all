apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-task-api.fullname" . }}
  labels:
    {{- include "go-task-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-task-api.selectorLabels" . | nindent 4 }}
