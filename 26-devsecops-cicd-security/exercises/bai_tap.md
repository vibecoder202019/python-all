# Bài tập — Module 26

## Bài 1

Thêm job GitHub Actions `iac-scan` dùng `aquasecurity/trivy-action` với `scan-type: config` trên thư mục `k8s/` (giả định). Viết YAML (không cần chạy).

## Bài 2

Sửa `sample-app/app.py`: thay MD5 bằng SHA-256. Chạy lại Bandit — xác nhận finding B324 hết.

## Bài 3

Viết `policy/conftest` kiểu checklist (markdown): 8 mục bắt buộc trước khi merge production.

## Bài 4

Giải thích ngắn: khác nhau SBOM vs vulnerability scan; vì sao cần cả hai.
