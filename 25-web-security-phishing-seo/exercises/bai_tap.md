# Bài tập — Module 25

## Bài 1

Thêm hàm `is_lookalike_domain(candidate: str, brand: str) -> bool` (ví dụ `paypa1.com` vs `paypal`) — dùng khoảng cách ký tự đơn giản (Levenshtein hoặc đếm khác biệt). Viết 5 test case.

## Bài 2

Cho dict headers thiếu CSP và HSTS — trả về danh sách remediation (string hướng dẫn cấu hình nginx/Ingress).

## Bài 3

Mở rộng `triage_ranking_drop`: nếu vừa có `security_issues` vừa `manual_actions`, ưu tiên `security_compromise` và ghi `secondary=manual_action`.

## Bài 4 (viết)

Viết 1 trang memo ½ trang: phân biệt “site bị hack → rớt Google” vs “Core Update”. Không mô tả cách tấn công SEO đối thủ.
