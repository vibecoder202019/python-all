# Bài tập Module 03

## Bài 1: Class Rectangle (Dễ)
Tạo class `Rectangle` với `width`, `height`, methods `area()`, `perimeter()`, `is_square()`.

## Bài 2: Class Stack (Trung bình)
Implement Stack với `push`, `pop`, `peek`, `is_empty`, `__len__`.

## Bài 3: Hệ thống thư viện (Trung bình)
- Class `Book(title, author, isbn, available=True)`
- Class `Library` với `add_book`, `borrow_book`, `return_book`, `search_by_author`

## Bài 4: ML Model Interface (Khó)
Tạo abstract class `BaseModel` với `train()`, `predict()`, `evaluate()`.  
Implement `KNNClassifier` đơn giản (dùng khoảng cách Euclidean).

Đáp án: [solutions/solutions.py](solutions/solutions.py)
