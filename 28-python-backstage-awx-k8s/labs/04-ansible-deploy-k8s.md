# Lab 04 — Ansible / K8s deploy

**Cách A — kubectl thuần:**

```bash
bash scripts/05-deploy-k8s-demo.sh
```

**Cách B — Ansible local (giống AWX):**

```bash
bash scripts/06-ansible-deploy-local.sh
```

**Cách C — qua Bridge + AWX thật** (sau khi cấu hình Job Template trỏ `deploy-app.yml`).
