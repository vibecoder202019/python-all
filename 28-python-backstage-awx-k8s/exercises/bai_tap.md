# Bài tập — Module 28

1. Dùng CLI `--demo` launch template 7 với `extra_vars` tự chọn; chụp `job_id`.
2. Viết 1 lệnh `curl` tạo task qua `POST /api/v1/jobs` (không dùng `/deploy`).
3. Giải thích vì sao Terraform tạo namespace còn Ansible tạo Deployment.
4. Thêm 1 Job Template vào `data/awx_fixture.json` và hiện trong `examples/01`.
5. (Nâng cao) Trỏ AWX thật (module 15) — launch 1 job và `wait` đến `successful`/`failed`.

Đáp án gợi ý: [solutions/dap_an.md](solutions/dap_an.md)
