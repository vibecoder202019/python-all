apiVersion: v1
kind: Service
metadata:
  name: agent-bridge
  namespace: ai-automation
  labels:
    app: agent-bridge
spec:
  selector:
    app: agent-bridge
  ports:
    - name: http
      port: 8090
      targetPort: 8090
