# Vault lab — Docker Compose
#
# Khoi dong:
#   docker compose up -d
#   export VAULT_ADDR='http://127.0.0.1:8200'
#   export VAULT_TOKEN='root'
#   vault status
#
# Dev mode trong container — CHI dung hoc tap.
